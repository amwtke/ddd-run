# 领域模型 · <项目/限界上下文名>

> 本文档是本项目领域模型的 **Single Source of Truth**。
>
> 所有 Superpowers spec、代码命名、测试描述都必须使用本文档定义的 Ubiquitous Language。
> 禁止在代码中出现与本文档术语不一致的命名。
>
> 本文件由 `/ddd-model` 管理。不要手工编辑(除非你知道自己在做什么)。

---

## 📌 状态
- [ ] 已完成事件风暴(`/ddd-storm`)
- [ ] 已完成初版领域建模(`/ddd-model`)
- [ ] 已生成至少一个 spec(`/ddd-spec`)
- [ ] 已有代码实现(Superpowers)

当前状态:**待初始化**。请先运行 `/ddd-storm <你的业务描述>`。

---

## 1. 限界上下文(Bounded Context)
- **名称**:<待填写>
- **职责**:<一句话说清这个上下文负责什么>
- **不负责**:<明确列出与之相邻但不属于本上下文的职责>

## 2. 统一语言(Ubiquitous Language)

> 这张表是全项目的命名宪法。代码、spec、测试、文档都必须使用这里的术语。

| 术语(中) | 术语(英) | 定义 | 类型 |
|---|---|---|---|
| _待 /ddd-model 填充_ | | | |

**类型枚举**:
- `Aggregate Root` — 聚合根,外部访问入口
- `Entity` — 聚合内部实体,有身份
- `Value Object` — 值对象,无身份,由属性定义
- `Domain Event` — 领域事件
- `Domain Service` — 领域服务(跨聚合无状态规则)

## 3. 聚合清单

> 每个聚合独立一节,由 `/ddd-model` 填充。

### 3.x <聚合名>(模板)
- **聚合根**:<AggregateRoot 名>
- **内部实体**:<Entity1>, <Entity2>
- **值对象**:<VO1>, <VO2>
- **不变式(Invariants)**:
  - INV-1: <必须始终成立的业务规则>
- **核心行为**:
  - `<方法签名>`:<前置条件 / 后置条件>
- **发出的领域事件**:
  - `<EventName>`:<何时发出,携带什么数据>

## 4. 跨聚合协调

> 跨聚合协作关系。默认采用最终一致性(领域事件)。

| 发起方 | 事件 | 消费方 | 触发动作 | 一致性 |
|---|---|---|---|---|
| _待填充_ | | | | |

## 5. 领域服务(Domain Services)

> 仅列跨聚合无状态规则。有状态规则应放在聚合内部。

| 服务名 | 方法 | 涉及聚合 | 职责 |
|---|---|---|---|
| _待填充_ | | | |

## 6. 建模决策记录(ADR)

> 每次重要建模决策在此记录,后续评审与迭代时用于说明"为什么这样建模"。

### ADR-0:初始化
- **决策**:使用 `ddd-run` 搭建 DDD + Superpowers harness
- **理由**:战略建模与战术实现分离,避免 AI 直接产出贫血模型
- **日期**:<YYYY-MM-DD>

### ADR-1:<待填充>
- **决策**:
- **理由**:
- **替代方案**:
- **日期**:

## 7. 下一步

- [ ] 运行 `/ddd-storm <业务描述>` 开始事件风暴
- [ ] 运行 `/ddd-model` 完成建模并更新本文件
- [ ] 对每个用例运行 `/ddd-spec <用例名>`
- [ ] 启动 Superpowers 按 spec 实现

---
*Managed by ddd-run + /ddd-model skill.*
